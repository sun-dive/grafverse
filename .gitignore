.DS_Store
*.log
node_modules
atoms/
sky-cache-*.json
sky-elements-*.json

# chain-follow caches (server-side, regenerated on demand)
*-tip.json

# Claude Code local settings — LOCAL ONLY, this repo is PUBLIC
.claude/

# the racers leaderboard's server-side chain-follow cache — written by racers.php,
# rebuilt from the chain whenever it is missing. Server state, never source.
racers-board.json
racebeta-board.json
brc226-tip.json

# the shared chain-call gate keeps its queue state here — runtime, never source
woc-gate.json
